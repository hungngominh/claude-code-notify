{
  "name": "claude-notify-app",
  "version": "2.0.0",
  "description": "Tray app to configure Claude Code notifications",
  "scripts": {
    "start": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
